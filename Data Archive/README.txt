Use:
Run GetFileLastWrite with $oldAge, $path and $outPath set
Then run File report with the $path as the same as $outPath as above and the new output path set
Veiw the outputted file to see all folders with all the contents not modified in the given time period
